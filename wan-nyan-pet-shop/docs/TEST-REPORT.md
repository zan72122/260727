# テストレポート

最終実行: Node v22.22.2 / Chromium 141（headless, SwiftShader）

| スイート | 件数 | 結果 | 実行時間 | コマンド |
|---|---:|---|---:|---|
| ユニット（ブラウザ不要） | 89 | ✅ 全件成功 | 約 0.6 秒 | `npm test` |
| E2E（実ブラウザ） | 15 | ✅ 全件成功 | 約 6 秒 | `npm run test:e2e` |
| スクリーンショット撮影 | 8 画面 | ✅ コンソールエラー 0 | 約 4 秒 | `npm run shots` |
| パフォーマンス計測 | 3 シナリオ | ✅ 60fps / 予算使用率 10% 未満 | 約 30 秒 | `npm run perf` |

## ユニットテスト（`tests/unit/`）

DOM を一切使わないので、CI でもブラウザなしで走ります。

### `rng.test.mjs`（7 件）
乱数の決定性・範囲・端点・シリアライズ復元。`int()` が両端を含むこと、
シード 0 でも縮退しないことを含みます。

### `pets.test.mjs`（19 件）
* 生成直後のステータスが範囲内であること
* 減衰レートがバランス表と一致すること（高級フードで実際に遅くなること）
* お世話の成否ルール（満腹・クールダウン・所持金不足・疲労・睡眠中のなでる）
* 性格によるなつき度の差
* 放置でけんこうが減り、世話で戻ること
* コンディション/きぶん/要求バブルの境界
* 4,000 ステップ歩き回っても座標が NaN にならず床から出ないこと
* **寝ている子をなでてもクールダウンが入ること**（連打でなつき度を上げられない — レビュー指摘の回帰テスト）

### `customers.test.mjs`（16 件）
品定めのスコアリング（種類・性格・健康）、価格がコンディション・なつき度・評判で
上がること、下限価格、辛抱の切れかた、選んだ子が居なくなったときの復帰、
来店間隔が上下限に収まること。
**お客さんがペットのそばまで歩いてからおむかえすること**（80px 以内・レビュー指摘の回帰テスト）と、
逃げ回る子を追いきれない場合に 10 秒で決断すること。

### `state.test.mjs`（24 件）
* 同じシードで完全に同じ展開になること
* 開店中以外はクリックが無効なこと
* 失敗したお世話でお金が減らないこと
* よごれ掃除、よごれの上限
* 動物病院（ペットは失われない）
* おむかえの入金・評判・アルバム記録
* 日締め（家賃の二重請求がないこと、`closeDay` の冪等性）
* 破産・エンディング（1 回だけ発火すること）
* 翌日処理（よごれのクリア、休息、ベッド上限を超えないこと、無人にならないこと）
* アップグレード購入と上限、おむかえの空きチェック
* イベントバス（購読者が例外を投げてもシミュレーションが止まらないこと）

### `save.test.mjs`（9 件）
セーブ→ロードの完全復元、復元後も同じ乱数系列で進むこと、壊れたセーブ・
別バージョン・ストレージ例外・ストレージ無しをすべて握り潰すこと、
復元後に生成するペットの ID が衝突しないこと。

### `loop.test.mjs`（8 件）
固定タイムステップの歩数、タブ復帰時のキャッチアップ上限（暴走しない）、
ポーズ中に描画は続くがシミュレーションは止まること、解除時に早送りされないこと、
2 倍速、パーティクルプールが増えないこと。

### `longplay.test.mjs`（5 件）
* **自動プレイのボットが 10 日間生き延び、評判とおむかえ数が伸びること**（バランス回帰テスト）
* 放置プレイでも状態が壊れないこと（そして損をすること）
* ランダムクリック 6 分ぶんで不変条件（NaN なし・範囲内・配列が無限に伸びない）を維持
* 長時間走らせてもコンディションが 0〜100 を出ないこと
* 1 日がきっちり 120 秒であること

## E2E テスト（`tests/e2e/game.e2e.mjs`）

実際の Chromium で、実際のマウス・キーボードイベントを使います。
UI のクリックは描画時に登録される矩形（`Regions`）の中心を狙うので、
当たり判定そのものの検証にもなっています。

1. 起動してゲームが公開され、キャンバスが白紙でないこと（色数を数える）
2. タイトルの「はじめから」で開店すること
3. 道具バーのクリックとキー `1`〜`5` の両方で道具が変わること
4. ペットを実際にクリックして給餌され、40円引かれ、選択状態になること
5. おそうじツールでよごれが消えること
6. `Esc` でシミュレーションが止まり、解除で再開すること
7. ループが実時間どおりに進むこと（0.7 秒で 0.3〜1.2 秒ぶん）
8. せいさん → お店じゅんび → 翌日 をボタンクリックだけで通せること
9. アップグレードが購入でき、所持金とレベルが正しく動くこと
10. ポーズで自動セーブ → リロード → 「つづきから」で所持金・日数・ペットが復元されること
11. **「セーブしてタイトルへ」→「はじめから」で本当に 1 日目からやり直せること**（レビュー指摘の回帰テスト）
12. **せいさん画面でポーズを押しても翌日が凍らないこと**（レビュー指摘の回帰テスト）
13. パーティクルを乱打してもプール上限（180）を超えないこと
14. ウィンドウサイズを変えても描画が続き、エラーが出ないこと
15. 実プレイ相当の操作を続けてもコンソールエラーが 0 件であること

## 手動確認（自動化していない部分）

* 音（Web Audio の合成音）は自動テストでは鳴らしていません。ミュート状態で走らせています。
* 各画面の見た目は `screenshots/` の 8 枚で確認しています。

## 再現手順

```bash
cd wan-nyan-pet-shop
npm test          # ユニット
npm run test:e2e  # E2E（Playwright 必要）
npm run verify    # 全部 + スクショ + 計測
```

## コードレビューで見つかった不具合と対応

読み取り専用のレビューを 1 回実施し、5 件の指摘を受けました。
プレイヤーに影響の大きい 4 件を修正し、それぞれに回帰テストを追加しています。

| 指摘 | 影響 | 対応 |
|---|---|---|
| 寝ている子をなでるとクールダウンが入らない（`pets.js`） | 連打でなつき度を 100 まで上げられ、価格・評判が壊れる | 修正（gentle 分岐でもクールダウンと世話回数を記録） |
| 「セーブしてタイトルへ」後の「はじめから」が古いセーブを再開し、家賃が請求されなくなる（`main.js`） | 1 日目から始まらない・無限に営業できてしまう | 修正（`startNewGame()` で必ず新規作成） |
| お客さんがペットまで歩かずに遠くからおむかえしてしまう（`customers.js`） | おむかえの演出が成立しない | 修正（品定め時に目標を設定し、実距離で判定・10秒でタイムアウト） |
| せいさん画面でポーズを押すと翌日が凍る（`main.js`） | 次の日が止まったまま始まる | 修正（開店中のみポーズ可能に） |
| 評判が 99.5〜100 のとき HUD が「100 / 100」と表示するがエンディングが出ない | 表示上の端数の丸め | 未対応（軽微・表示のみのため見送り） |
