# パフォーマンス計測レポート

> このファイルは `npm run perf` (tools/perf.mjs) が自動生成します。数値は実測値です。
> 生成日時: 2026-07-26T19:30:48.810Z

## 計測環境

| 項目 | 値 |
|---|---|
| レンダラ | Chromium headless, SwiftShader software rasteriser |
| 解像度 | 960x600 CSS px（devicePixelRatio 1、内部 960×600 論理ピクセル） |
| 計測時間 | 各シナリオ 8 秒 |
| UA | `Mozilla/5.0 (X11; Linux x86_64) AppleWebKit/537.36 (KHTML, like Gecko) HeadlessChrome/141.0.0.0 Safari/537.36` |

Software rendering is a worst case; hardware-accelerated desktop browsers are considerably faster.

## 起動

| 指標 | 実測 |
|---|---|
| DOMContentLoaded | 123.4 ms |
| load | 123.5 ms |
| 最初のフレームが出るまで | 198.5 ms |

外部アセット（画像・音声・フォント・CDN）をひとつも読み込まないため、起動はネットワークではなく
JS のパースとルームレイヤーの初回描画だけで決まります。

## シナリオ別フレーム計測

frame 列は rAF の実測間隔（vsync で 60 fps 上限）、render / sim 列は 1 フレーム内で実際に
消費した処理時間です。60 fps の予算は 16.67 ms なので、余力は budget 列で読めます。

| シナリオ | ペット | パーティクル | fps | frame avg (ms) | frame p95 (ms) | render avg (ms) | render p95 (ms) | sim avg (ms) | 60fps予算の使用率 |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| タイトル画面（アイドル） | 2 | 0 | 60 | 16.665 | 16.9 | 0.492 | 0.7 | 0.006 | 3% |
| 通常プレイ（開店中・お客さん来店あり） | 2 | 0 | 60 | 16.723 | 16.9 | 0.921 | 1.2 | 0.03 | 5.7% |
| ストレス（ペット6匹・よごれ8個・パーティクル連続発生） | 6 | 180 | 60 | 16.669 | 17.2 | 1.531 | 2.2 | 0.075 | 9.6% |

最も重い「ストレス（ペット6匹・よごれ8個・パーティクル連続発生）」でも 1 フレームあたりの自前処理は平均 1.531 ms、
p95 で 2.2 ms。ソフトウェアラスタライザ上でも 60 fps の予算の
9.6% しか使っていません。

## シミュレーション単体

レンダリングを止めて、ゲーム内 60 秒ぶんを一気に回した結果です。

| 指標 | 実測 |
|---|---|
| 60 秒ぶんの実行時間 | 15.5 ms |
| 1 ステップ (1/30 秒) あたり | 0.0086 ms |
| 実時間に対する倍率 | 約 3871× |

固定タイムステップ 30 Hz なので、シミュレーションはフレームレートに左右されません。
自動テストが 10 日ぶんのプレイを 1 秒未満で回せるのはこの軽さのおかげです。

## 効いている最適化

1. **店内の背景をオフスクリーンにキャッシュ**（`src/render/room.js`）
   壁・床・窓・棚・ラグ・ドアは変化しないので、起動時に一度だけ 960×600 のオフスクリーン
   キャンバスへ描き、毎フレームは 1 回の `drawImage` で転送しています。実測で
   毎フレーム描き直すと 0.517 ms、キャッシュ転送なら 0.008 ms
   （**約 69× 高速**）。
2. **パーティクルは固定長プール**（`src/render/fx.js`）
   `Particles` は起動時に 180 個を確保し、以降は使い回すだけ。プレイ中の
   パーティクル生成でヒープが増えないため、GC 由来のカクつきが起きません。
   計測中のヒープは 2.8 / 2.5 / 2.6 MB で安定していました。
3. **固定タイムステップ + キャッチアップ上限**（`src/core/loop.js`）
   1 フレームで進めるシミュレーションは最大 5 ステップ。タブを長時間隠しても
   復帰時に大量のステップが走る「死のスパイラル」が起きません。
4. **devicePixelRatio は 2 で頭打ち**（`src/render/renderer.js`）
   高 DPI 環境でも塗る量が 4 倍を超えないようにしています。
5. **当たり判定は毎フレーム作り直す矩形リストのみ**（`src/render/hud.js`）
   DOM を一切使わず、UI もキャンバスに直接描いているためレイアウト計算やリフローが発生しません。

## 消費リソース

| 項目 | 値 |
|---|---|
| 外部リクエスト | 0（画像・音声・フォント・スクリプトすべて自前） |
| JS ヒープ（計測中） | title: 2.8 MB / typical: 2.5 MB / stress: 2.6 MB |
| コンソールエラー | なし |
